# Copy to .env (do not commit .env)
#
# Pydantic AI Gateway
# Sign up: https://gateway.pydantic.dev/
# Create a key in the Keys page.

PYDANTIC_AI_GATEWAY_API_KEY=paig_your_key_here

# Optional: choose the model to use via the Gateway.
# Examples:
#   gateway/openai:gpt-5
#   gateway/openai:gpt-5-mini
#   gateway/openai-responses:gpt-5
PDFMDER_MODEL=gateway/openai:gpt-5
