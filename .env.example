# Copy to .env (do not commit .env)

# Optional: Logfire write token (cloud logging). We default to console-only.
LOGFIRE_TOKEN=your_logfire_write_token_here

# Default setup: use OpenAI directly (no Pydantic AI Gateway required).
# Create a key: https://platform.openai.com/
OPENAI_API_KEY=sk-your-openai-key-here

# Model name used by OpenAI Responses API (direct OpenAI or Azure deployment name).
# Examples:
#   gpt-5
#   gpt-4o-mini
PDFMDER_MODEL=gpt-5

# Optional: use a custom OpenAI base URL.
OPENAI_BASE_URL=https://api.openai.com/v1

# Azure OpenAI (optional). When AZURE_OPENAI_ENDPOINT is set, uses Azure deployment.
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com
AZURE_OPENAI_API_KEY=your-azure-openai-key-here
AZURE_OPENAI_DEPLOYMENT=your-deployment-name
AZURE_OPENAI_API_VERSION=preview
