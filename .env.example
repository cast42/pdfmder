# Copy to .env (do not commit .env)

# Optional: Logfire write token (cloud logging). We default to console-only.
LOGFIRE_TOKEN=your_logfire_write_token_here

# Default setup: use OpenAI directly (no Pydantic AI Gateway required).
# Create a key: https://platform.openai.com/
OPENAI_API_KEY=sk-your-openai-key-here

# Model routing string used by Pydantic AI.
#
# Examples (OpenAI):
#   openai:gpt-5
#   openai:gpt-4o-mini
#
# Later (when you want) you can also use other providers:
#   anthropic:claude-sonnet-4-5   (requires ANTHROPIC_API_KEY)
#   google-gla:gemini-2.5-pro     (requires GOOGLE_API_KEY)
#   gateway/openai:gpt-5          (requires PYDANTIC_AI_GATEWAY_API_KEY)
PDFMDER_MODEL=openai:gpt-5

# Force direct OpenAI calls (disable gateway/openai routing) unless set to 0.
PDFMDER_FORCE_OPENAI=1

# Pydantic AI Gateway (optional)
PYDANTIC_AI_GATEWAY_API_KEY=paig_your_key_here
